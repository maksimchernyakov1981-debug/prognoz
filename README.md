# Мини-приложение для Telegram бота МЦ "Прогноз"

## Описание

Мини-приложение (Web App) для Telegram бота медицинского центра "Прогноз". Предоставляет удобный интерфейс для доступа к основным функциям бота.

## Установка и настройка

### 1. Размещение файлов

Разместите папку `webapp` на веб-сервере с поддержкой HTTPS. Telegram требует, чтобы Web App были доступны по HTTPS.

### 2. Настройка URL в боте

Добавьте в файл `.env` переменную окружения:

```env
WEBAPP_URL=https://your-domain.com/webapp/index.html
```

Замените `https://your-domain.com` на реальный URL вашего веб-сервера.

### 3. Настройка бота через BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/mybots`
3. Выберите вашего бота
4. Выберите "Bot Settings" → "Menu Button"
5. Нажмите "Configure Menu Button"
6. Введите текст кнопки (например, "📱 Мини-приложение")
7. Введите URL вашего мини-приложения (например, `https://your-domain.com/webapp/index.html`)

### 4. Локальная разработка

Для локальной разработки можно использовать простой HTTP сервер:

```bash
# Python 3
cd webapp
python -m http.server 8000

# Или используйте ngrok для создания HTTPS туннеля
ngrok http 8000
```

Затем используйте URL от ngrok в настройках бота.

## Структура файлов

```
webapp/
├── index.html      # Основной HTML файл мини-приложения
└── README.md       # Этот файл
```

## Функциональность

Мини-приложение предоставляет:

- 💼 Услуги и цены
- 📅 Запись на прием
- 📋 Подготовка к анализам
- 💬 Контакты
- ℹ️ О клинике
- ⚙️ Настройки

## Интеграция с ботом

Мини-приложение отправляет данные обратно в бот через Telegram Web App API. Обработчик данных находится в `handlers/webapp_handler.py`.

## Технические требования

- HTTPS (обязательно для Telegram Web App)
- Современный браузер с поддержкой JavaScript
- Telegram Web App API

## Примечания

- Мини-приложение автоматически адаптируется под тему Telegram (светлая/темная)
- Использует Telegram Web App API для интеграции с ботом
- Поддерживает кнопку "Назад" в Telegram

