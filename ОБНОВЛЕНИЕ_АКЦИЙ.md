# Решение проблемы обновления акций в мини-приложении

## Проблема
Акции не обновляются в мини-приложении после изменения в боте.

## Причины
1. Файл не скопирован в `webapp/data/medical/promotions.json`
2. Файл не загружен на Vercel
3. Браузер кэширует старые данные

## Решение

### Шаг 1: Проверка и копирование файла

Запустите скрипт синхронизации:
```powershell
powershell -ExecutionPolicy Bypass -File sync_webapp_data.ps1
```

Или вручную:
```powershell
# Создаем папку если не существует
New-Item -ItemType Directory -Force -Path "webapp\data\medical" | Out-Null

# Копируем файл
Copy-Item "data\medical\promotions.json" -Destination "webapp\data\medical\promotions.json" -Force
```

### Шаг 2: Загрузка на Vercel

После копирования файла нужно загрузить обновленную папку `webapp/` на Vercel:

**Вариант A: Через Vercel CLI**
```bash
cd webapp
vercel --prod
```

**Вариант B: Через веб-интерфейс Vercel**
1. Откройте проект на Vercel
2. Перейдите в настройки деплоя
3. Нажмите "Redeploy" или загрузите файлы вручную

**Вариант C: Через Git (если настроен)**
```bash
git add webapp/data/medical/promotions.json
git commit -m "Обновление акций"
git push
```

### Шаг 3: Очистка кэша в мини-приложении

После обновления на Vercel:

1. **Закройте и откройте мини-приложение заново**
2. **Или нажмите кнопку "🔄 Обновить" в разделе акций**
3. **Или очистите кэш браузера** (Ctrl+F5)

## Автоматическое обновление

Мини-приложение автоматически обновляет акции:
- При открытии раздела акций
- При возврате в главное меню
- Каждые 2 минуты в фоне
- При фокусе на окне (если прошло больше 30 секунд)

## Проверка

1. Измените акцию в админ-панели бота
2. Проверьте, что файл скопирован: `webapp/data/medical/promotions.json`
3. Загрузите на Vercel
4. Откройте мини-приложение и проверьте акции

## Отладка

Откройте консоль браузера (F12) в мини-приложении и проверьте:
- `✅ Акции обновлены, версия: ...` - данные загружены
- `📊 Количество акций: ...` - количество загруженных акций
- `❌ Ошибка загрузки акций` - проблема с загрузкой файла

## Важно!

**После каждого изменения акций в боте:**
1. Файл автоматически копируется в `webapp/data/medical/` ✅
2. **НО нужно загрузить обновленную папку `webapp/` на Vercel!** ⚠️

Без загрузки на Vercel изменения не появятся в мини-приложении!

