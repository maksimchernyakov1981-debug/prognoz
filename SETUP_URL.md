# Инструкция по настройке URL для мини-приложения

## Вариант 1: Локальная разработка (с ngrok)

### Шаг 1: Установите ngrok
1. Скачайте ngrok с https://ngrok.com/download
2. Распакуйте в удобную папку
3. Зарегистрируйтесь на ngrok.com (бесплатно)

### Шаг 2: Запустите локальный сервер
Откройте терминал в папке `webapp` и запустите:

```bash
# Python 3
python -m http.server 8000

# Или используйте другой порт, например 3000
python -m http.server 3000
```

### Шаг 3: Запустите ngrok
В новом терминале:

```bash
# Если используете порт 8000
ngrok http 8000

# Если используете порт 3000
ngrok http 3000
```

### Шаг 4: Скопируйте HTTPS URL
После запуска ngrok вы увидите что-то вроде:
```
Forwarding  https://abc123.ngrok.io -> http://localhost:8000
```

Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)

### Шаг 5: Добавьте в .env файл
Создайте файл `.env` в корне проекта (если его нет) и добавьте:

```env
WEBAPP_URL=https://abc123.ngrok.io/webapp/index.html
```

**Важно:** URL должен заканчиваться на `/webapp/index.html`

---

## Вариант 2: Продакшн (реальный веб-сервер)

### Шаг 1: Загрузите файлы на сервер
1. Загрузите папку `webapp` на ваш веб-сервер
2. Убедитесь, что файлы доступны по HTTPS (обязательно!)

### Шаг 2: Проверьте доступность
Откройте в браузере:
```
https://your-domain.com/webapp/index.html
```

Если страница открывается - всё правильно!

### Шаг 3: Добавьте в .env файл
Создайте файл `.env` в корне проекта и добавьте:

```env
WEBAPP_URL=https://your-domain.com/webapp/index.html
```

**Примеры:**
- `WEBAPP_URL=https://prognozmc.com/webapp/index.html`
- `WEBAPP_URL=https://www.prognozmc.ru/webapp/index.html`
- `WEBAPP_URL=https://bot.prognozmc.com/webapp/index.html`

---

## Вариант 3: Использование GitHub Pages (бесплатно)

### Шаг 1: Создайте репозиторий на GitHub
1. Создайте новый репозиторий
2. Загрузите туда только папку `webapp`

### Шаг 2: Включите GitHub Pages
1. Зайдите в Settings → Pages
2. Выберите ветку (обычно `main`)
3. Сохраните

### Шаг 3: Получите URL
URL будет выглядеть так:
```
https://your-username.github.io/repository-name/webapp/index.html
```

### Шаг 4: Добавьте в .env
```env
WEBAPP_URL=https://your-username.github.io/repository-name/webapp/index.html
```

---

## Создание .env файла

Если файла `.env` нет, создайте его в корне проекта:

```env
# Токен бота (уже должен быть)
BOT_TOKEN=your_bot_token_here

# ID администраторов (уже должны быть)
ADMIN_IDS=123456789,987654321

# URL мини-приложения (ДОБАВЬТЕ ЭТУ СТРОКУ)
WEBAPP_URL=https://your-domain.com/webapp/index.html
```

---

## Проверка настройки

После настройки URL:

1. Перезапустите бота
2. Откройте бота в Telegram
3. Нажмите кнопку "📱 Мини-приложение" в главном меню
4. Мини-приложение должно открыться

---

## Важные замечания

⚠️ **HTTPS обязателен!** Telegram не работает с HTTP для Web App

⚠️ **URL должен быть полным** - включая `https://` и путь к файлу

⚠️ **Файл должен быть доступен** - проверьте в браузере перед настройкой

⚠️ **После изменения .env** - перезапустите бота

---

## Решение проблем

### Мини-приложение не открывается
- Проверьте, что URL доступен в браузере
- Убедитесь, что используется HTTPS
- Проверьте, что путь правильный (`/webapp/index.html`)

### Ошибка "Invalid URL"
- Убедитесь, что URL начинается с `https://`
- Проверьте, что нет лишних пробелов в .env файле
- Убедитесь, что URL заканчивается на `/webapp/index.html`

### Бот не видит изменения
- Перезапустите бота
- Проверьте, что .env файл в правильной папке (корень проекта)
- Проверьте синтаксис .env файла (нет ли ошибок)

